import React from "react";
import { Text } from "react-native";
import { typo } from "~/styles";

export const SecondHandTransactionScreen = () => {
  return <Text style={[typo.title, { flex: 1 }]}>중고거래</Text>;
};
