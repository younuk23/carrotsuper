import React from "react";
import { Text } from "react-native";
import { typo } from "~/styles";

export const VillageLifeScreen = () => {
  return <Text style={[typo.title, { flex: 1 }]}>준비 중인 서비스입니다.</Text>;
};
